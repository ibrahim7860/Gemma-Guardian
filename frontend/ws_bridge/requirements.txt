fastapi>=0.110
uvicorn>=0.27
websockets>=12.0
# Phase 2: Redis subscribe + fakeredis-backed tests
redis>=5.0
fakeredis>=2.20
# Shared contracts (pulled in transitively, but pin floors here for clarity)
jsonschema>=4.18
referencing>=0.30
pydantic>=2.5
PyYAML>=6.0
pytest>=8.0
pytest-asyncio>=0.23
pytest-playwright>=0.5
httpx>=0.27   # for ASGI test client
